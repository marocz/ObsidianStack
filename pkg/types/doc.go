// Package types defines shared Go types used by both the agent and server.
// These are the canonical in-memory representations of pipeline health data,
// separate from the protobuf wire format.
package types
