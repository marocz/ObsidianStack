# Claude Code project management files (not for public repo)
ARCHITECTURE.md
CLAUDE_CODE_GUIDE.md
PROJECT_STATUS.json
.claude/

# Compiled binaries
bin/
dist/

# Go build cache
*.test
*.out

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment / secrets
.env
.env.*
!.env.example
config.yaml
!config.example.yaml

# config/ directory — real configs may contain passwords; only examples are committed
config/agent.yaml
config/server.yaml

# Cluster deployment session notes — may contain passwords / tokens
deploy/cluster/session-notes.md

# Local override files — never commit these
# Use these filenames for local testing; they are git-ignored by design:
#   deploy/cluster/values.local.yaml  (local cluster overrides)
#   docker-compose.override.yml       (local compose overrides)
#   config/agent.local.yaml           (local agent config)
*.local.yaml
docker-compose.override.yml

# Node / UI
ui/node_modules/
ui/dist/
ui/.vite/

# SQLite database files
*.db
*.db-shm
*.db-wal

# Test coverage
coverage.out
coverage.html